/**
 * Renders the Home page container.
 *
 * The component outputs an empty, full-height centered flex container that
 * provides the page's root layout and responsive background (light by default,
 * dark in dark mode).
 *
 * @returns A JSX element: a div used as the page root with centered flex layout and theme-aware background.
 */
export default function Home() {
   return (
      <div className="flex flex-col flex-1 items-center justify-center bg-zinc-50 font-sans dark:bg-black">

      </div>
   );
}
